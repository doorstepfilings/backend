PORT=4000
API_PREFIX=api
APP_NAME=DoorstepFilings
FRONTEND_URL=http://localhost:3000
NODE_ENV=development

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USERNAME=doorstep
DB_PASSWORD=DoorstepFilings@2026
DB_DATABASE=doorstep_main

JWT_SECRET=supersecret
JWT_EXPIRES_IN=7d

MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=krishna.radio2pir@gmail.com
MAIL_PASSWORD=lsstvxbryqeftnmi
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=krishna.radio2pir@gmail.com
MAIL_FROM_NAME="Doorstepfilings"


RAZORPAY_KEY_ID=rzp_test_SLxXyZTjic6PoZ
RAZORPAY_KEY_SECRET=c1flzOtIbZGHxFkv41yNHZT4


# This was inserted by `prisma init`:
# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

DATABASE_URL="mysql://doorstep:DoorstepFilings@2026@127.0.0.1:3306/doorstep_main"
